import { CashoutErrorMessageComponent } from './cashout-error-message.component';

describe('CashoutErrorMessageComponent', () => {
  let component;

  beforeEach(() => {
    component = new CashoutErrorMessageComponent();
  });

  it('should init component', () => {
    expect(component).toBeTruthy();
  });
});
