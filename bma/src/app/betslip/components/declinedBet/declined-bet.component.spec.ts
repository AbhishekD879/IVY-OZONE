import { DeclinedBetComponent } from '@betslip/components/declinedBet/declined-bet.component';

describe('DeclinedBetComponent -', () => {
  let component: DeclinedBetComponent;

  beforeEach(() => {
    component = new DeclinedBetComponent();
  });

  it('constructor', () => {
    expect(component).toBeTruthy();
  });
});
