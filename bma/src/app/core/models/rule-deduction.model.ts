// initially created for racing events
export interface IRuleDeduction {
  deduction: string;
  deductionType: string;
  fromDate: string;
  id: string;
  marketId: string;
  siteChannels: string;
  toDate: string;
}
