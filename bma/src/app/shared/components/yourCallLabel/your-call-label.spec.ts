import { YourCallLabelComponent } from './your-call-label.component';

describe('YourCallLabelComponent', () => {
  let component: YourCallLabelComponent;

  beforeEach(() => {
    component = new YourCallLabelComponent();
  });

  it('should create component instance', () => {
    expect(component).toBeTruthy();
  });
});
