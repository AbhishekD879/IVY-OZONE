

xdescribe('InplayOutrightCardComponent', () => {
 /* let component: InplayOutrightCardComponent;
  let fixture: ComponentFixture<InplayOutrightCardComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ InplayOutrightCardComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(InplayOutrightCardComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });*/
});
