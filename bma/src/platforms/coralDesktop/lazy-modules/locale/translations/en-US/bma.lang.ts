import { bma  as appBma } from '@app/lazy-modules/locale/translations/en-US/bma.lang';

export const bma = {
  ...appBma,
  miniGamesError: 'Mini Games currently unavailable, please try again later.',
};
