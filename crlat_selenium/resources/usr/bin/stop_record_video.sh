#!/usr/bin/env bash
#recoders=$(ps aux | grep flvrec| awk 'NR==1 {print $2}')
#if [ -n $recoders ]; then
#    kill $recoders
#fi
#vnc4server -kill :${DKR_ID}
#killall Xvnc4
#avconv -i /tmp/video.flv -threads 4 -vf scale=360:600 /tmp/output.webm -y
#exit 0
echo disabled video