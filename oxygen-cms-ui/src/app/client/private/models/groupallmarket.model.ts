export interface GroupAllMarket {
  id: string;
  marketId: string;
  name: string;
  defaultName: string;
  nameOverride: string;
  enabled: boolean;
  viewType: string;
  collapsed: boolean;
  maxDisplay: number;
}
