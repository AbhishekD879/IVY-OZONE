
import { Base } from './base.model';

export interface YourCallStaticBlock extends Base {
  title_brand: string;
  title: string;
  lang: string;
  enabled: boolean;
  htmlMarkup: string;
  fiveASide: boolean;
}
