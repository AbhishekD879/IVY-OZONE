package com.ladbrokescoral.oxygen.trendingbets.dto;

public class PersonalizedRecord extends TrendingItem {
  // Verify if other fields are needed otherwise we can remove and use the same TrendingItem
}
